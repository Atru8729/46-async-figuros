class Figura {
    static randomFiguros(amount) {
        const f = [];

        for () {
            f.push({})
        }
        return f;
    }

    static size(width, height) {

    }
}

module.exports = Figura;